name: Miloco Fixed
version: "v0.1.2"
slug: miloco-fixed
description: Xiaomi miloco streamer (SSL patched build, P2P host_network, ingress-ready)
arch:
  - aarch64
build: true
init: false
host_network: true
ingress: true
ingress_port: 8000
ingress_entry: "/"
panel_admin: false
environment:
  MILOCO_SERVER_STORAGE_DIR: /data
volumes:
  - /data
options: {}
schema: {}
